const pessoa = { nome: "Maria", idade: 30 };

console.log(`Idade de ${pessoa.nome}: ${pessoa.idade}`);

pessoa.email = "maria@email.com";
console.log(`Email adicionado: ${pessoa.email}`);
console.log("Objeto atualizado:", pessoa);